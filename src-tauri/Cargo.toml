[package]
name = "habitt"
version = "2.0.0"
description = "Habitt. — privacy-first habit tracker"
authors = ["opngo"]
license = "MIT"
edition = "2021"

[lib]
name = "habitt_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
strip = true
panic = "abort"
