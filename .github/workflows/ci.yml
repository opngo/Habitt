name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  web:
    name: Build & smoke-test web UI
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
      - name: Headless smoke test (jsdom)
        run: npm run smoke
      - uses: actions/upload-artifact@v4
        with:
          name: habitt-web
          path: dist/
